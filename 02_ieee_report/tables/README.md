# Tables

Place all standalone table source files here.

## Format

Each table as a separate `.tex` file.

Use `booktabs` style where appropriate.

## Naming

`tab_<section>_<number>_<topic>.tex`

## Instructions

Include tables in `main.tex` or section files using `\input{tables/XX_name.tex}`.
